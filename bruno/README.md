Bruno is a git friendly API testing tool that stores requests as plain text files.

Open the Bruno GUI and choose the bruno folder at the repository root to load the requests.

Make sure the services are running via docker compose before testing.
